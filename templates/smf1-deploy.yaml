apiVersion: v1
kind: Service
metadata:
  name: open5gs-smf1
  labels:
    epc-mode: smf1
spec:
  selector:
    epc-mode: smf1
  ports:
    - protocol: TCP
      port: 80 
      targetPort: 80  
---  
apiVersion: apps/v1 # for versions before 1.9.0 use apps/v1beta2
kind: Deployment
metadata:
  name: open5gs-smf1-deployment
  labels:
    epc-mode: smf1
spec:
  replicas: 1
  selector:
    matchLabels:
      epc-mode: smf1
  template:
    metadata:       
      annotations:
        sidecar.istio.io/inject: "true"
      labels:
        epc-mode: smf1
    spec:   
      containers:
      - name: smf1
        image: "{{ .Values.open5gs.image.repository }}:{{ .Values.open5gs.image.tag }}"
        imagePullPolicy: {{ .Values.open5gs.image.pullPolicy }}
        command: ["/bin/sh", "-c"]
        args:
        - open5gs-smfd -c /open5gs/config-map/smf1.yaml;
        volumeMounts:
          - name: open5gs-smf1-config
            mountPath: /open5gs/config-map/smf1.yaml
            subPath: "smf1.yaml"                                       
      volumes:
        - name: open5gs-smf1-config
          configMap:
            name: open5gs-smf1-config                
